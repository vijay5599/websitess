import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IronCore Fitness - Best Gym in Indiranagar, Bangalore",
  description: "IronCore Fitness offers weight training, personal training, cardio programs, and fat loss coaching in Indiranagar, Bangalore. Open 5 AM to 10 PM. Book your trial session today!",
  keywords: "gym, fitness, training, Indiranagar, Bangalore, weight training, personal training, cardio",
  icons: {
    icon: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
